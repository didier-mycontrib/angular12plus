<div class="mainSideNav flexRowNoWrap">

  <!-- main level menu , always visible -->
    <div class="flexItem main-level-menu" >
       <div class="my-menu-item" *ngFor="let md of menuDefs"   (click)="onNav(md)" 
              [class.selected]="md === selectedMenuDef"
              (mouseenter)="onSubMenu(md)" >{{md.label}}
        </div> 
     
    </div>

     <!-- sub level menu , visible or not --> 
    <div class="flexItem sub-level-menu" 
         [style.display]="subMenuDefs?'block':'none'">
        <div *ngFor="let smd of subMenuDefs"  class="my-menu-item" (click)="onNav(smd)" >{{smd.label}}
        </div> 
    </div>
</div>