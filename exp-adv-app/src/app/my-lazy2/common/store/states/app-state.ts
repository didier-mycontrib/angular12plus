import { UserSession } from "../../model/user-session";
/*
export interface AppState {
     count: number ;
     pub : boolean ;
     session: UserSession;
}
*/
//facultatif, juste nécessaire pour 
//constructor(private store: Store<AppState>) {...}
//plutot que 
//constructor(private store: Store<any>) {...}
